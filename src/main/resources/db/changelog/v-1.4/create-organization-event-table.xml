<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.0.xsd">
    <changeSet id="create-organization-event-table" author="VadimMor">
        <sql>
            CREATE TABLE IF NOT EXISTS public.organization_event
            (
                organization_id bigserial NOT NULL,
                event_id bigserial NOT NULL,
                CONSTRAINT organization_event_pkey PRIMARY KEY (organization_id, event_id),
                CONSTRAINT fkcf9kin3eb0qfk1kv4yruv0gn8 FOREIGN KEY (organization_id)
                REFERENCES public.organization (organization_id) MATCH SIMPLE
                ON UPDATE NO ACTION
                ON DELETE NO ACTION,
                CONSTRAINT fkkdfa9kg2f3p9s3l42nf8fi6nx FOREIGN KEY (event_id)
                REFERENCES public.event (event_id) MATCH SIMPLE
                ON UPDATE NO ACTION
                ON DELETE NO ACTION
            )
        </sql>
    </changeSet>
</databaseChangeLog>