<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.0.xsd">
    <changeSet id="create-basket-favorite-tables" author="Dmitrii">
        <sql>
            CREATE TABLE IF NOT EXISTS public.basket
            (
                user_id  bigserial NOT NULL,
                ticket_id bigserial NOT NULL,
                CONSTRAINT user_ticket_pkey PRIMARY KEY (user_id, ticket_id),
                CONSTRAINT jasijjtsgnsdsakgsknnslndfnj FOREIGN KEY (user_id)
                    REFERENCES public.users (user_id) MATCH SIMPLE
                    ON UPDATE NO ACTION
                    ON DELETE NO ACTION,
                CONSTRAINT gtbwebgsldhbsuhhgdgudsghuds FOREIGN KEY (ticket_id)
                    REFERENCES public.ticket (ticket_id) MATCH SIMPLE
                    ON UPDATE NO ACTION
                    ON DELETE NO ACTION
            );

            CREATE TABLE IF NOT EXISTS public.favorites
            (
                user_id  bigserial NOT NULL,
                event_id bigserial NOT NULL,
                CONSTRAINT user_event_pkey PRIMARY KEY (user_id, event_id),
                CONSTRAINT asfklaknangsjviassanasndfjk FOREIGN KEY (user_id)
                    REFERENCES public.users (user_id) MATCH SIMPLE
                    ON UPDATE NO ACTION
                    ON DELETE NO ACTION,
                CONSTRAINT hashhfsfhkjaskjfhsahuyhgwth FOREIGN KEY (event_id)
                    REFERENCES public.event (event_id) MATCH SIMPLE
                    ON UPDATE NO ACTION
                    ON DELETE NO ACTION
            );
        </sql>
    </changeSet>
</databaseChangeLog>